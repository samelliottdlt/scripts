# Agent instructions

This is a **public** repo of generic, reusable personal scripts run via `s <name>`.
Read and follow [`skills/s-scripts/SKILL.md`](skills/s-scripts/SKILL.md) before making changes.
That's the same file installed as the `s-scripts` agent skill.

Hard rule: never commit work-specific, private, or personal information, secrets, or
vendor-licensed files.
